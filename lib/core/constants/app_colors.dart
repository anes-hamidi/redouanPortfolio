import 'package:flutter/material.dart';

/// Centralized color palette for Cheb Redouane Portfolio
abstract class AppColors {
  /// Luxury Midnight Navy Blue (Primary Brand Color)
  static const Color navy = Color(0xFF0A1128);

  /// Traditional Algerian Gold (Secondary Accent Color)
  static const Color gold = Color(0xFFD4AF37);

  /// Warm Cream Light Background
  static const Color cream = Color(0xFFFAF9F6);

  /// Luxury Dark Charcoal Background for Services
  static const Color darkCharcoal = Color(0xFF0F162A);

  /// Dark Slate Card Fill
  static const Color cardDarkSlate = Color(0xFF1E293B);

  /// Dark Slate Card Border
  static const Color cardBorderSlate = Color(0xFF334155);

  /// Body Text Charcoal Color
  static const Color bodyText = Color(0xFF4A4A4A);

  /// Deep Hero Fallback Background
  static const Color heroBackground = Color(0xFF030712);
}
